export const cameraConfig = {
  fov: 60,
  aspect: window.innerWidth / window.innerHeight,
  near: 0.1,
  far: 3000,
};

export const devCameraConfig = {
  fov: 70,
  aspect: window.innerWidth / window.innerHeight,
  near: 0.1,
  far: 3000,
};
